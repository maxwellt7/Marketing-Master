# Optional - for persistent storage (app works without this using in-memory storage)
DATABASE_URL=postgresql://user:password@host:port/database

# Server port (default: 5000)
PORT=5000

# Node environment
NODE_ENV=development
